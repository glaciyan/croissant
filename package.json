{
    "private": true,
    "workspaces": [
        "web",
        "glacuu-components"
    ],
    "scripts": {
        "web": "yarn workspace components run build && yarn workspace web run dev"
    }
}
